export enum OfferingGroup {
	CORE = "core",
}

export enum OfferingName {
	CABLETV = "cabletv",
	AIRTIME = "airtime",
	DATA_BUNDLE = "databundle",
}

export enum PaymentMethod {
	OFFLINE = "offlinePay",
}

export enum phedcDetails {
	ARREARS = "Arrears (NGN)",
	ENERGY_VALUE = "Energy Value (NGN)",
	PRELOAD = "Preload (NGN)",
	VAT = "VAT (NGN)",
}

export enum PaymentMethod {
	WALLET = "Wallet",
}

export enum Currency {
	NGN = "NGN",
}

export enum Status {
	FAILED = "Failed",
	SUCCESS = "Success",
}

export enum Codes {
	ITEM = "01",
}
